# ForgeClaw runtime is OpenAI-only for the current stabilization target.
# API keys are normally entered in Settings and stored locally in the browser.
# This file documents the accepted key shape for operators who keep env samples with deployments.
OPENAI_API_KEY=sk-...or-sk-proj-...
